filters:
  and:
    - type == "study-cycle"
    - status == "active"
views:
  - type: table
    name: Active Cycle
    order:
      - file.name
      - subject
      - cycle_start
      - test_date
      - status
    sort:
      - property: test_date
        direction: ASC
